# Server

Estructura prevista:

- `actions/`: comandos Server Actions por contexto
- `services/`: reglas y coordinacion de dominio
- `repositories/`: acceso a Prisma/SQL
- `policies/`: autorizacion y restricciones reutilizables
